# AGENTS.md — 에이지 오브 도미니언 (Age of Dominion, 가칭)

이 문서는 이 저장소의 사양이자 작업 규약이다. 매 세션 시작 시 다시 읽고 따른다.
원 기획서: `docs/design.md` (사용자 제공 게임 디자인 문서). 이전 3D RTS(Astragene)는 Git 태그
`astragene-rts-final`에 보존되어 있으며 이 프로젝트와 무관하다.

## 0. 개요
- 장르: 실시간 멀티플레이 영토 확장 전략(io 게임) + 시대 발전 + 채팅 외교.
- 이번 목표 범위: **Blitz 모드 알파** (기획서 13장 0~2단계). Dominion(대전략) 모드는 범위 밖이며,
  Blitz 완성 전 Dominion 코드는 작성하지 않는다.
- 핵심 판타지: 친구들과 채팅으로 동맹을 맺고 배신하며 구석기 부족에서 미래 문명까지 키운다.
- 디자인 필러: ① 말이 곧 무기다 ② 시대가 판을 뒤집는다 ③ 한 판 15~30분.

## 1. 기술 스택 (변경 시 먼저 질문)
- Godot 4.6.3 **표준(비 .NET) 빌드**, **GDScript 2.0**, 렌더러 **GL Compatibility**(웹 export 대상).
- 2D 전용. 대형 맵은 소유권 텍스처 + 셰이더 1회 드로우로 그린다(TileMapLayer 대량 사용 금지).
- 네트워크: **리슨 서버 릴레이형 결정론적 락스텝**. 호스트가 서버를 겸한다.
  데스크톱은 ENetMultiplayerPeer, 웹은 WebSocketMultiplayerPeer. 서버는 두 전송을 동시에 연다.
  같은 프로젝트를 `--server`로 헤드리스 전용 서버로도 실행할 수 있어야 한다.
- 테스트: GdUnit4 6.2.1 (`tools/setup_gdunit.ps1`), 헤드리스 시뮬레이터 `tools/sim_runner.gd`.

## 2. 절대 규칙
- [R1] 결정론: `core/`는 Node를 상속하지 않는 순수 데이터 클래스(RefCounted)만 둔다.
  float 금지(정수·×1000 고정소수점), 난수는 `SimRng`만, Dictionary 순회 결과에 로직이 의존하면 안 된다
  (정렬된 id 배열로 순회), 시간·OS·렌더 API 호출 금지. 시뮬레이션은 고정 틱(10틱/초)에서만 진행한다.
- [R2] 단방향 의존: presentation/net → core (읽기). core는 presentation/net을 모른다.
  core가 외부에서 받는 것은 Intent뿐, 내보내는 것은 상태 조회와 이벤트 목록뿐이다.
- [R3] 네트워크로는 Intent(와 채팅)만 보낸다. 타일 소유권을 동기화하지 않는다. 재접속·디싱크 복구는
  시뮬레이션 스냅샷 + 이후 턴으로 한다.
- [R4] 모든 core 시스템은 `tests/`에 GdUnit4 테스트를 동반한다. 결정론 테스트(같은 시드·같은 Intent → 같은 해시)는 항상 통과해야 한다.
- [R5] 한 번에 한 Phase. 시작 전 PLAN.md에 변경 파일과 이유, 완료 조건을 쓴다. 커밋 메시지 `phase-N: 요약`.
- [R6] 데이터 주도: 시대·건물·특성·조약·지형·AI 성향 수치는 `data/*.json`에 둔다(정수). 코드에 하드코딩하지 않는다.
  수치는 시험값이며 헤드리스 AI 대전과 플레이테스트로 조정한다.
- [R7] 외부 코드 복사 금지(OpenFront.io는 AGPL). 구조와 아이디어만 참고한다. 에셋·폰트는 라이선스 확인분만.
- [R8] 락스텝이므로 모든 클라이언트가 전체 상태를 가진다. 비공개 DM·조약 제안 표시는 "표시상의" 비공개이며
  README에 이 한계를 명시한다. 채팅 본문은 서버가 수신자에게만 중계한다.

## 3. 저장소 구조
```
project.godot
core/            # 결정론 시뮬레이션 (RefCounted, 정수)
  ai/            # 봇(FSM), 국가(유틸리티 AI)
data/            # rules.json 등 룰셋 데이터
net/             # 서버, 클라이언트, 프로토콜, 채팅 중계
presentation/    # 씬, 맵 렌더러, 셰이더, UI
assets/          # 폰트(라이선스 동봉), 아이콘
tests/           # GdUnit4
tools/           # sim_runner, 빌드/실행 스크립트
docs/            # 설계 문서, 검증 기록
```

## 4. Blitz 규칙 요약 (세부 수치는 data/rules.json)
- 맵: 타일 격자(기본 400×240), 지형 = 바다/평원/숲/언덕/산. 절차 생성(정수 노이즈, 시드 공유).
- 시작: 10초 스폰 선택 → 반경 5 영토 + 초기 인구.
- 인구(=병력): 로지스틱 성장 `Δ = r·pop·(1−pop/cap)`, cap = 영토 × 시대 계수 + 도시 보너스.
- 금: 영토·시장·무역으로 수입. 건물 구매(가격은 보유 수에 따라 상승).
- 공격: 병력 비율(%)을 정해 인접 무주지/적 영토에 투입 → 경계에서 BFS로 번짐. 타일 비용 = 지형 × (적 밀도) ÷ 시대 전투력 비율.
- 시대 8개(구석기~미래): 게이지(시간+영토+연구소, 최하위 +30%, 세계 시대보다 2단계 뒤처지면 +50%)가 차면 전환, 특성 3택 1.
  시대 차 1당 전투력 +25%, 최대 +60%. 수도·요새 방어 ×1.5.
- 해상(시대 5 근세부터 항구 → 상륙선), 미사일·핵(시대 7 현대, 사일로), 해킹·궤도 타격(시대 8).
- 조약: 불가침, 동맹, 휴전, 무역, 조공, 기술 공유. 조약 중 공격 = 조약 파기 + "배신자"(60초, 방어 −20%, AI 신뢰 급락, 제안 쿨다운).
  불가침은 예고 파기(30초 유예) 시 배신이 아니다.
- 승리: 육지 80% 점유(개인 또는 동맹 합산) 또는 시간 종료 시 최대 영토.
- AI: 봇(단순 확장 FSM), 국가(유틸리티 AI + 성향 프로필 + 신뢰도 기억, 조약 제안·수락·배신).
  난이도 = 판단 주기·차선책 확률(쉬움 15초/20%, 보통 6초/10%, 어려움 3초/0%, 어려움만 자원 +10% — 기획서 7.2).
- 채팅: 전체(3초 쿨다운)/1:1 DM/동맹/관전자/시스템, 조약 카드(수락/거절), `@` 지도 핑(Alt+클릭), 속도 제한 5초 3개, 금칙어 필터(비공개 방 기본 OFF), 개인 음소거.

## 5. 검증 명령
```
./tools/setup_gdunit.ps1
godot --headless --import
godot --headless -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a tests/
godot --headless -s tools/sim_runner.gd -- --seed=42 --minutes=20
```
